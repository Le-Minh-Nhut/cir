[project]
name = "cir-research"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "torchvision",
    "entmax==1.3",
    "hydra-core",
    "omegaconf",
    "numpy",
    "pillow",
    "tqdm",
    "transformers>=4.57.0,<6",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "ruff",
    "mypy",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
